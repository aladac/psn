# Speak Text

Speak the provided text using the personality MCP server.

Use the `mcp__personality__speak` tool to vocalize the text.

**Arguments:** $ARGUMENTS

If no text is provided, ask the user what they want me to say.
