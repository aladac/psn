# Personality Status

Show the current personality system status.

1. Read `personality://cart` resource to show current cart
2. Run `psn voices` to show available voices
3. Show the MCP server configuration from `~/.mcp.json`

Provide a summary of the personality system state.
