# List Available Voices

List available voice models for personality speech synthesis.

Run `psn voices` via the Bash tool to show available Piper TTS voice models:

```bash
cd /Users/chi/Projects/_PERSONALITY_/personality && uv run psn voices
```

Display the voice model names and their file locations.
