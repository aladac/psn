# Documentation Index Status

Show the current status of the documentation index.

Run `psn docs status` to view:
- Docs path being indexed
- Index database location
- Number of indexed documents
- Number of content chunks

## Example Usage

```
/psn:docs-status
```

If no documents are indexed, run `psn docs index` to index the docs directory.
